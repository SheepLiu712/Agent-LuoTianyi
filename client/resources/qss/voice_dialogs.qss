/* ========================================
   Voice Dialogs — 对话框通用样式
   ======================================== */

/* ── 对话框全局 ── */
QDialog {
    background-color: {{CARD}};
}

/* ── 标签页（TabWidget） ── */
QTabWidget::pane {
    border: none;
    background-color: transparent;
}
QTabBar::tab {
    font-size: 15px;
    padding: 8px 20px;
    margin-right: 2px;
    border: none;
    border-bottom: 2px solid transparent;
    color: {{TEXT_SECONDARY}};
    background-color: transparent;
}
QTabBar::tab:selected {
    color: {{TIANYI_BLUE}};
    border-bottom: 2px solid {{TIANYI_BLUE}};
    font-weight: 600;
}
QTabBar::tab:hover:!selected {
    color: {{TEXT_PRIMARY}};
    border-bottom: 2px solid {{BORDER}};
}

/* ── 按钮（通用） ── */
QPushButton#primaryButton {
    background-color: {{TIANYI_BLUE}};
    color: {{TEXT_ON_PRIMARY}};
    border: none;
    border-radius: 8px;
    padding: 10px 24px;
    font-size: 15px;
    font-weight: 600;
}
QPushButton#primaryButton:hover {
    background-color: {{TIANYI_BLUE_DEEP}};
}
QPushButton#primaryButton:pressed {
    background-color: #2980B9;
}
QPushButton#primaryButton:disabled {
    background-color: #E2E8F0;
    color: {{TEXT_DISABLED}};
}

/* ── 文字按钮 ── */
QPushButton#textButton {
    background-color: transparent;
    color: {{TIANYI_BLUE_DEEP}};
    border: none;
    padding: 4px 8px;
    font-size: 14px;
    text-align: left;
}
QPushButton#textButton:hover {
    color: {{TIANYI_BLUE}};
}

/* ── 危险按钮 ── */
QPushButton#dangerButton {
    background-color: {{ERROR}};
    color: {{TEXT_ON_PRIMARY}};
    border: none;
    border-radius: 8px;
    padding: 10px 24px;
    font-size: 14px;
}
QPushButton#dangerButton:hover {
    background-color: #DC2626;
}

/* ── 输入框（通用） ── */
QLineEdit, QTextEdit, QSpinBox, QDateEdit, QTimeEdit {
    border: 1px solid {{BORDER}};
    border-radius: 6px;
    padding: 8px 10px;
    font-size: 14px;
    color: {{TEXT_PRIMARY}};
    background-color: {{CARD}};
}
QLineEdit:focus, QTextEdit:focus, QSpinBox:focus, QDateEdit:focus, QTimeEdit:focus {
    border: 1px solid {{TIANYI_BLUE}};
    background-color: {{CARD}};
}
QLineEdit:disabled, QTextEdit:disabled {
    background-color: #F1F5F9;
    color: {{TEXT_DISABLED}};
}
QLineEdit::placeholder, QTextEdit::placeholder {
    color: {{TEXT_DISABLED}};
}

/* ── 下拉框 ── */
QComboBox {
    border: 1px solid {{BORDER}};
    border-radius: 6px;
    padding: 8px 10px;
    font-size: 14px;
    color: {{TEXT_PRIMARY}};
    background-color: {{CARD}};
}
QComboBox:focus {
    border: 1px solid {{TIANYI_BLUE}};
}
QComboBox::drop-down {
    border: none;
    width: 28px;
}
QComboBox::down-arrow {
    width: 10px;
    height: 10px;
}
QComboBox QAbstractItemView {
    border: 1px solid {{BORDER}};
    border-radius: 6px;
    background-color: {{CARD}};
    selection-background-color: {{TIANYI_BLUE_GLOW}};
    selection-color: {{TEXT_PRIMARY}};
    padding: 4px;
}

/* ── 复选框 ── */
QCheckBox {
    font-size: 14px;
    color: {{TEXT_PRIMARY}};
    spacing: 6px;
}
QCheckBox::indicator {
    width: 18px;
    height: 18px;
    border-radius: 4px;
    border: 1px solid {{BORDER}};
}
QCheckBox::indicator:checked {
    background-color: {{TIANYI_BLUE}};
    border-color: {{TIANYI_BLUE}};
}

/* ── 标签（通用） ── */
QLabel#dialogTitle {
    font-size: 20px;
    font-weight: 700;
    color: {{TIANYI_BLUE}};
}
QLabel#dialogDesc {
    font-size: 13px;
    color: {{TEXT_SECONDARY}};
}
QLabel#sectionLabel {
    font-size: 14px;
    font-weight: 600;
    color: {{TEXT_PRIMARY}};
}

/* ── 列表（动态等） ── */
QListWidget {
    background-color: {{CARD}};
    border: 1px solid {{BORDER}};
    border-radius: 6px;
    padding: 4px;
    outline: none;
}
QListWidget::item {
    border: none;
    border-radius: 6px;
    padding: 6px;
    margin: 2px;
}
QListWidget::item:selected {
    background-color: {{TIANYI_BLUE_GLOW}};
    border-left: 3px solid {{TIANYI_BLUE}};
}
QListWidget::item:hover:!selected {
    background-color: #F1F5F9;
}

/* ── 对话框底部按钮行 ── */
QDialogButtonBox QPushButton {
    background-color: {{TIANYI_BLUE}};
    color: {{TEXT_ON_PRIMARY}};
    border: none;
    border-radius: 6px;
    padding: 8px 20px;
    font-size: 14px;
}
QDialogButtonBox QPushButton:hover {
    background-color: {{TIANYI_BLUE_DEEP}};
}

/* ── 消息框 ── */
QMessageBox {
    background-color: {{CARD}};
}
QMessageBox QLabel {
    color: {{TEXT_PRIMARY}};
    font-size: 14px;
}
QMessageBox QPushButton {
    min-width: 80px;
    padding: 6px 16px;
    border-radius: 6px;
    font-size: 13px;
}

/* ── 动态对话框 ── */
QDialog#dynamicsDialog {
    background-color: #F6F7F9;
}

QLabel#dynamicsTitle {
    font-size: 20px;
    font-weight: 700;
    color: #243447;
}

QLabel#dynamicsMeta {
    font-size: 12px;
    color: #667481;
}

QLabel#dynamicsContent {
    color: #243447;
}

QLabel#dynamicsError {
    font-size: 12px;
    color: #A35C00;
}

QLabel#dynamicsStatus {
    color: #A35C00;
}

QLabel#dynamicsSectionLabel {
    font-weight: 600;
    color: #243447;
}

QDialog#dynamicsDialog QPushButton {
    background: {{TIANYI_BLUE}};
    color: {{TEXT_ON_PRIMARY}};
    border: none;
    border-radius: 6px;
    padding: 8px 12px;
}
QDialog#dynamicsDialog QPushButton:hover {
    background: {{TIANYI_BLUE_DEEP}};
}
QDialog#dynamicsDialog QPushButton:disabled {
    background: {{BORDER}};
    color: {{TEXT_DISABLED}};
}
QDialog#dynamicsDialog QPushButton#publishBtn {
    font-weight: 700;
    padding: 8px 16px;
}

QWidget#detailWidget {
    background: {{CARD}};
    border: 1px solid {{BORDER}};
    border-radius: 6px;
}
QLabel#dynamicDetailName {
    color: {{TEXT_PRIMARY}};
    font-size: 14px;
    font-weight: 600;
}
QLabel#dynamicDetailSeparator {
    background: {{BORDER}};
}
QLabel#dynamicDetailContent {
    color: {{TEXT_PRIMARY}};
    font-size: 14px;
}
QLabel#dynamicDetailPlaceholder {
    color: {{TEXT_DISABLED}};
    font-size: 13px;
    padding: 20px;
}

/* 动态编辑器 */
QDialog#dynamicEditor {
    background: #F6F7F9;
}

QLabel#editorTitle {
    font-size: 18px;
    font-weight: 800;
    color: #243447;
}

QDialog#dynamicEditor QPushButton#editorAction {
    background: transparent;
    color: #1296DB;
    border: none;
    padding: 8px 10px;
    font-weight: 700;
}
QDialog#dynamicEditor QPushButton#editorAction:hover {
    background: #E6F4FE;
    border-radius: 6px;
}
QDialog#dynamicEditor QPushButton#editorAction:disabled {
    color: #8AA7B8;
}

QDialog#dynamicEditor QTextEdit {
    background: #FFFFFF;
    border: 1px solid #D5DEE7;
    border-radius: 6px;
    padding: 10px;
    font-size: 15px;
}

/* 动态列表项 */
QLabel#dynamicItemHeader {
    font-weight: 700;
    color: #243447;
}

QLabel#dynamicItemPreview {
    color: #334155;
    font-size: 13px;
}

/* 浮动发布按钮 */
QPushButton#addDynamicButton {
    background: transparent;
    border: none;
    padding: 0;
}
QPushButton#addDynamicButton:hover {
    background: transparent;
}
