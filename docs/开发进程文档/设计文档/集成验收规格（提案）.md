# 集成验收规格（提案，未实现）

- 状态：**设计提案，待评审**。只记录验收范围与证据形态，不执行验收。
- 关联工单：Issue [#89](https://github.com/SheepLiu712/Agent-LuoTianyi/issues/89)（30 按行为不变量与架构边界完成现有链路验收）
- 相关规范：总 SPEC 第 8 节（A1—A9、8.3—8.6、8.7 证据要求）、9.2 节

## 1. 要解决的问题

架构重构的最终验收必须**同时**证明：两个 Agent 业务接口收束、依赖方向收束、全部用户链路与九类 clock action 行为等价、以及真实依赖的未验证项被明确列出。各子工单完成不等于端到端验收完成。

## 2. 前置条件

- 26/27/28（机械边界）已完成；
- 29（contract 收束）已合入：生产调用图只剩两个 Agent 接口；
- 07—25 全部迁移完成。

## 3. 证据矩阵（按 SPEC 8.7 的最低形态）

| 范围 | 证据形态 | 观察入口 |
| --- | --- | --- |
| A1—A2（接口收束/内部不泄漏） | 依赖扫描 + 运行时对象图 | `AgentRuntime.get_agent` 与包导出检查 |
| A3—A5（认知/行动单入口、外部职责外置） | 生产调用图 + 跨模块集成测试 | 公开两接口；Adapter/stage/world 调用点扫描 |
| A6—A7（双轨删除、只依赖公开领域协议） | 静态扫描（无旧代理/内部类型外部依赖） | 代码库扫描报告 |
| A8（内部异步维护归 Agent） | 集成测试 | REFLECT 链路 |
| A9（包所有权/依赖方向） | import 方向扫描 + 目录检查 | `server/src` 扫描 |
| 8.3 聊天全信号 | 集成/少量 e2e | 聊天入口（含 typing/选图/deadline/旧 revision/部分消费） |
| 8.4 触摸 | 集成 | 触摸入口（预制音频、独立恢复、失败丢弃、去重） |
| 8.5 登录 | 集成 | 首次/当日/重复登录与通知 claim |
| 8.6 九类 clock action | 可控时钟逐项触发 | `WorldRuntime.tasks` + 生效配置；区分纯机械与认知链 |

## 4. 记录与判定规则

- 每项记录：命令、收集数、通过/跳过/失败、环境；**跳过项不得写成通过**；
- 真实 LLM/VLM/TTS/唱歌/GPU/设备/生产环境单独列为人工验收清单，保留可复核输出且不记录密钥/隐私数据；
- 重投不得产生重复回复、记忆、动态、日记、日程、事件或学歌任务；
- 发现行为冲突时创建/重开对应缺陷切片，**不在验收 PR 中塞入大修复**。

## 5. 未决问题

1. 依赖扫描如何固化（脚本 / CI job / 人工清单）？
2. e2e 的“少量核心场景”具体选哪些（避免与模块测试重复）？
3. 人工验收清单的载体（Issue 附件 / 进度文档 / 独立验收记录）？

## 6. 明确不包含

- 本提案不运行验收、不新增测试、不修改代码或文档状态。
- 不发布版本、不推送 Issue 状态。
