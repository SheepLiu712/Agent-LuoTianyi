# #89 行为不变量验收记录

- **工单**：[#89（切片 30）按行为不变量与架构边界完成现有链路验收](https://github.com/SheepLiu712/Agent-LuoTianyi/issues/89)
- **验收基线**：`refactor/agent`，切片 29 合并后（#169 `221ae49` / #171 `63fc474`）
- **验收依据**：同目录 `行为不变量与副作用清单（#89 验收前置）.md`（§1 G1–G10、§2–§18 逐链路、附录 A/B/C）
- **可复现命令**：
  - 架构边界：`cd server && python scripts/check_architecture_boundaries.py --verbose`
  - 默认全量回归：`cd server && python -m pytest tests -q --tb=short`
  - 分层测试、覆盖率及外部 E2E：见 `server/tests/README.md`
- **结论**：原 #89 验收时架构边界静态检查 **7/7 PASS**，白名单行为回归 **1018 passed / 2 skipped**。2026-09-19 已取消白名单并把全部测试迁入 `unit`、`integration`、`e2e` 三层；当前默认全量回归 **1306 passed / 17 skipped**（2 项可选真实 LLM、15 项真实外部 E2E），单元测试对重构范围的行覆盖率为 **66.52%**。当前不变量与测试的权威映射见 `server/tests/INVARIANTS.md`。

---

## 1. 全局不变量（G1–G10）验收结果

| 不变量 | 判定 | 证据 |
|---|---|---|
| G1 外部认知只经两个业务入口 | **已验收** | `check_architecture_boundaries.py` B1（exclusive）：全仓仅 4 处调用点，全部位于 `stage/chat_stage.py:444/502`、`stage/world_stage.py:181/236`；B6 确认无 AgentRuntime 旧代理与 `*_for_pipeline` 残留（仅保留 `generate_topic_reply_for_pipeline`，2 文件 allowlist） |
| G2 world 不依赖 Agent/Stage | **已验收（含 1 项已知偏差 F1）** | B2 PASS：world 对 `src.agent`/`src.stage` 的引用全部位于 `if TYPE_CHECKING:` 内（22 个 world 模块逐个核对）；F1 见下节 |
| G3 副作用只经两通路 | **已验收** | B4 PASS：`agent/handlers/action/**` 无 `database_manager`/`requests.`/`session.add`；4 个 action handler 均以 `EffectRef` 交付（`dynamic.py:32` DYNAMIC_POST、`dynamic_reply.py:33` DYNAMIC_COMMENT、`song_learning.py:39` SONG_LEARNING_JOB、`write_diary.py:31`） |
| G4 handler 不直连基础设施 | **已验收** | B3 PASS：`agent/handlers/**` 对 `src.world`/`EventStore`/`event_store`/`subconscious`/`database_manager`/`dynamic_store` 命中 0 |
| G5 Stage 持交互 context | **已验收** | `integration/stage/test_chat_stage.py`、`integration/stage/test_world_stage.py`、`integration/stage/test_world_stage_contracts.py`、`integration/agent/test_context.py`、`unit/agent/test_request_isolation.py` |
| G6 单次失败停止、不引入账本 | **已验收** | `integration/agent/test_handler_dispatch.py`、`integration/stage/test_chat_reply_settlement.py`、`unit/agent/test_plan_delivery_failure.py`、`unit/agent/test_output_delivery_failure.py`、`unit/agent/test_execution_isolation.py`；遗留 `agent/ledgers/*` 文件无生产调用者 |
| G7 world 无实时输出通道 | **已验收** | B5 PASS：world 内无 `OutputSink`/`Say`/`Sing`/`output_channel` 命中；`integration/stage/test_world_stage_contracts.py` 验证世界链路只产持久效果 |
| G8 去重归各自边界 | **已验收** | 逐链路见 §2（主动提醒 claim、动态来源唯一性、日记每日唯一、学歌 `already learned`、动态互动目标唯一） |
| G9 领域类型冻结、变更经裁决 | **已验收** | `unit/domain/test_stimulus_registered_types_contract.py`、`test_stimulus_text_message_contract.py`、`test_stimulus_value_types_contract.py`、`test_handling_report_contract.py`、`test_realization_contract.py`，并由 `integration/agent/test_facade_contract.py`、`test_handler_dispatch.py` 验证跨组件拒绝；本切片唯一的领域扩大是 N2 裁决的 `DiaryPlanningDue.owner_user_id`（#167） |
| G10 失败不冒充成功 | **已验收** | `integration/agent/test_handler_dispatch.py`、`integration/world/test_world_task_dynamics.py`、`integration/stage/test_world_settlement_wiring.py`、`unit/agent/test_plan_delivery_failure.py` |

### F1：world → `system_runtime.agent_runtime` 只读属性耦合（已知偏差，非本次引入）

- **事实**：5 个 world 模块通过 `getattr(system_runtime, "agent_runtime", None)` 读取 Agent 侧属性，共 7 处：
  - `world/world_runtime.py:257/267`（`character_runtimes`、`default_character_id`，用于枚举/取默认角色以注册任务）
  - `world/citywalk/task.py:171/220`（`default_character_id`、`vector_store`，用于构造 `CitywalkRuntimeService`）
  - `world/dynamic_interaction/task.py:288`、`world/get_new_songs/task.py:96`、`world/learn_sing_songs/task.py:126`（`default_character_id`，用于取得本角色 WorldStage）
- **定性**：**只读属性访问，不调用任何 Agent 业务方法**；`git show 9ed8d1c:` 逐文件比对显示与旧架构**完全同源、数量一致**（同一模式计数在新旧提交上逐文件相同），**非本次重构引入**。
- **影响**：违反 G2 的「world 不依赖 Agent 内部」的严格表述，但属装配层读取（角色枚举 + 向量库句柄），不影响任何副作用正确性；已在 `check_architecture_boundaries.py` 的 B7 中以 allowlist 形式**冻结**，一旦扩散到新文件即 FAIL。
- **建议**（独立工单，不在本次范围）：world 侧改为读自身角色配置，`vector_store` 由 `SystemRuntime` 以显式端口提供。

---

## 2. 逐链路验收结果（§2–§18）

| # | 链路 | 判定 | 当前分层回归证据 |
|---|---|---|---|
| 2 | 聊天主链 | 已验收 | `integration/stage/test_concurrent_handling.py`、`test_chat_reply_settlement.py`、`e2e/chat/test_production_stage_wiring.py` |
| 3 | 交互控制与取消/断线 | 已验收 | `integration/stage/test_chat_stage.py`、`test_concurrent_handling.py`、`integration/websocket/test_websocket_adapter.py`、`e2e/chat/test_touch_stage.py` |
| 4 | 主动提醒（17） | 已验收 | `integration/stage/test_proactive_due_dispatch.py`、`integration/system/test_login_stage_routing.py`、`unit/world/test_world_task_proactive_topic_check.py` |
| 5 | 首次登录欢迎（16） | 已验收 | `integration/stage/test_first_login_scheduling.py`、`integration/system/test_login_stage_routing.py`、`e2e/chat/test_first_login_welcome.py` |
| 6 | 反思与记忆（13/14） | 已验收 | `integration/stage/test_concurrent_handling.py`、`integration/agent/test_context.py`、`integration/persistence/test_subconscious_memory.py` |
| 7 | 明确记忆意图（12） | 已验收 | `integration/persistence/test_intentional_memory_storage.py`、`unit/agent/test_intentional_memory.py` |
| 8 | 慢召回多计划（11a/11b） | 已验收 | `integration/stage/test_chat_reply_settlement.py`、`integration/stage/test_concurrent_handling.py`、`unit/agent/test_slow_recall_staged_reply.py` |
| 9 | citywalk（21） | 已验收 | `integration/world/test_world_task_dynamics.py`、`integration/stage/test_world_settlement_wiring.py`、`unit/world/test_world_task_citywalk.py` |
| 10 | VCPedia（22） | 已验收 | `integration/agent/test_song_knowledge_acceptance.py`、`unit/world/test_world_task_vcpedia_new_songs.py`；真实抓取在 `e2e/external/test_world_live.py`（显式启用） |
| 11 | 学歌（23） | 已验收 | `integration/world/test_world_task_dynamics.py`、`unit/world/test_world_task_learn_sing_songs.py`；真实基础设施在 `e2e/external/test_infrastructure_singing.py`（显式启用） |
| 12 | 动态互动（24） | 已验收 | `integration/world/test_world_task_dynamics.py`、`integration/agent/test_dynamic_operations.py`、`unit/agent/test_dynamic_interaction.py` |
| 13 | 日记（25） | 已验收 | `integration/world/test_world_task_diary.py`、`integration/agent/test_diary_operations.py`、`unit/agent/test_diary_writing.py` |
| 14 | B站事件/过期清理（26/28） | 已验收 | `integration/world/test_world_task_event_cleanup.py`、`unit/world/test_world_task_bili_event_update.py`、`unit/world/test_world_runtime.py`；真实抓取在 `e2e/external/test_world_live.py`（显式启用） |
| 14b | QQ 凭据刷新（27） | **部分（仅装配与调度）** | 任务装配与后台刷新由 `integration/system/test_admin_runtime.py`、`unit/world/test_world_clock.py`、`unit/world/test_world_runtime.py` 覆盖；真实凭据路径仍属 C1 |
| 15 | 媒体链（09） | 已验收 | `integration/websocket/test_websocket_adapter.py`、`integration/stage/test_concurrent_handling.py`、`unit/infrastructure/test_media_resolution.py`；真实 VLM 在 `e2e/external/test_infrastructure_image_understanding.py`（显式启用） |
| 16 | 输出与发送面 | 已验收 | `integration/agent/test_handler_dispatch.py`、`integration/websocket/test_websocket_adapter.py`、`e2e/chat/test_first_login_welcome.py`、`e2e/chat/test_touch_stage.py` |
| 17 | 调度与生命周期 | 已验收 | `integration/lifecycle/test_system_runtime_shutdown.py`、`test_facade_inflight_shutdown.py`、`integration/runtime/test_runtime_shutdown.py`、`integration/world/test_world_stage_registry.py` |
| 18 | 失败与部分效果语义 | 已验收 | `integration/agent/test_handler_dispatch.py`、`integration/stage/test_world_settlement_wiring.py`、`integration/world/test_world_task_dynamics.py`、`unit/domain/test_handling_report_contract.py` |

**2026-09-19 更新**：测试白名单和目录级私有 fixture 已移除；原延迟/遗留测试现由根 `tests/conftest.py` 与公共 support 模块统一收集。默认全量测试会收集全部三层测试，真实外部依赖仅由显式 marker 跳过。

---

## 3. 本次验收新增的可复现证据

`server/scripts/check_architecture_boundaries.py`（只读静态检查，退出码 0 = 通过）：

```
[B1-业务入口唯一] 调用点仅限 Stage: PASS
[B2-world-不得运行时依赖认知层] PASS（TYPE_CHECKING 例外已排除）
[B3-handler-无基础设施直连] PASS
[B4-action-不得自造副作用] PASS
[B5-world-无实时输出] PASS
[B6-旧管线代理零残留] PASS（allowlist 3 条：保留的 generate_topic_reply_for_pipeline 定义与唯一调用）
[B7-已知偏差-不得扩散] PASS（allowlist 7 条，即 F1）
结果: PASS —— 全部静态边界检查通过
```

## 4. 未验收与后续（不阻塞本工单）

| 编号 | 项 | 归属 |
|---|---|---|
| C1 | 真实外部依赖：VCPedia 抓取、B 站 feed、QQ 音乐下载与凭据、高德、对象存储/URL 媒体 | 人工验收（需真实网络与凭据） |
| C2 | 真实 LLM/VLM 生成质量与真机客户端发送语义 | 人工验收 |
| C3 | Windows 时序抖动：`integration/stage/test_concurrent_handling.py`、`integration/stage/test_chat_stage.py` 的 deadline 断言曾偶发提前（当前全量复跑通过，断言未削弱） | 持续观察 |
| C4 | F1：world → `agent_runtime` 只读属性耦合（已冻结，不得扩散） | 独立工单 |
| C5 | 历史模块 Ruff 风格债与延迟测试集 fixture 局限 | **已处理**：首阶段重构范围 Ruff/Black 纳入门禁；白名单删除，fixture 上移至根目录，全部测试可统一收集 |
