# Godot Windows 客户端

## 0.1.3 用户批准增量（2026-09-21）

只改Godot客户端。未登录改为约480×690的纯白QQ式小窗，透明窗口+Godot圆角面板、无渐变/裁切遮罩；顶部仅菜单/关闭，菜单为设置服务器/日志/项目GitHub反馈。服务器须现有公钥接口验证并保存成功才切换；底部忘记密码调用原邀请码重置，同窗注册/重置、返回清敏感信息。登录后恢复系统窗框。

账号历史按服务器隔离，记住登录状态与自动登录独立，不保存密码；旧配置迁移、过期回密码登录、退出清当前凭据并保留历史。业务/UI登录存储只依赖StorageService，浏览器能力独立注入。文本气泡按实际排版贴合、90%宽度上限，语音/状态不撑宽；图片保留原比例、小图不放大、只限宽不限高，阅读锚点和缓存策略保持。

按实际客户端回归与GPU/导出窗口验收后打包agentluo-0.1.3.zip，保留旧包，仅本地提交；未实测环境明确记录。具体行为及失败规则见[0.1.3 interface](../../项目说明/项目架构与接口（spec）/接口文档/client_godot/release-013.md)。

- 功能基线：`dev@79ae2c0`。
- 需求来源：2026-09-18 用户确认的《Godot Windows 客户端重建与功能对齐计划》。
- 总体设计：[客户端总体设计](../../项目说明/项目架构与接口（spec）/Godot客户端总体设计.md)。
- 当前 interface：[Godot 客户端](../../项目说明/项目架构与接口（spec）/接口文档/client_godot/README.md)。
- 完成事实：[开发进度](../开发进度/Godot-Windows客户端.md)。

## 目标

交付可替换现有 Python 桌面端的 Windows 10/11 x64 客户端，面向普通集显、8 GB 内存电脑。保留当前用户功能，重新设计角色和聊天界面，最终提供当前用户范围的安装程序。开发期间保留旧客户端；验收后切换正式交付入口，旧发布包保留用于回退。

## 用户已确定的体验

1. 左侧角色、右侧聊天，初始比例 45:55，可调整并保存。
2. 安静的陪伴氛围，浅色低对比面板、深色文字；控件主题色 #66CCFF，聊天排版以用户确认的 QQ 风格效果图为准，白色天依气泡、浅蓝用户气泡、圆形头像。
3. 复用现有背景、模型、头像；静态背景，不新增插画、背景音乐或持续粒子。
4. 角色保留眨眼、呼吸、表情、动作、视线、触摸和口型；支持滚轮缩放、右键拖动、保存及重置，左键仍用于触摸。
5. 普通窗口，使用系统标题栏与三键（2026-09-21用户选择，替代自绘标题栏）。未登录只显示账户区（初始 660×800，最小 480×640）；登录后展开左角色、右聊天（初始 1200×800，最小 960×640），退出登录重新收起，关闭即退出。
6. 默认自动播放回复，可调音量及回放消息语音；重放支持播放、暂停、继续、停止，不支持拖动进度。完整语音持久缓存，仅用户手动清理，不自动淘汰；新在线语音抢占本地重放，口型同步但保留当前表情。
7. Enter 发送、Shift+Enter 换行；中文输入法组词时 Enter 不发送。支持文字选择复制、单张图片粘贴预览和显式发送。
8. 长文本换行；翻看历史时不被新消息强制拉到底部；历史分页保留阅读位置。
9. 先交付带真实模型和模拟消息的可运行样板，由用户确认后再展开账户、设置及动态页面。
10. 换端时重新登录和配置，历史及偏好从服务端恢复；不导入旧 token、API key、图片或语音缓存。
11. 默认服务器沿用旧客户端的 `https://www-api.u3493359.nyat.app:11664`；用户已保存的有效自定义地址优先。
12. 左侧导航为聊天、动态、设置、日志四个居中入口，音量位于底部工具栏。语音缓存清理与退出登录均在设置中；退出应用使用主窗系统关闭按钮。不新增聊天正文数据库。

## 功能对齐验收矩阵

下表是需求范围，不表示已实现；完成事实只写入进度文档。

| 能力 | 基线行为和观察入口 |
| --- | --- |
| 账户 | 登录、注册、自动登录、邀请码重置、自定义服务器、退出；`/auth/*` |
| 文本 | `user_text`、`user_typing`、主动发言，发送状态、系统提示、连续输入 |
| 网络可靠性 | `user_auth`、`negative_ack_v1`、心跳、重连、原 ID 重试、终止拒绝、`DELIVERY_UNCERTAIN` |
| 图片 | 选择/取消、发送、预览、本地缓存及历史图片补下载 |
| 回复 | 按 UUID 聚合，保持文本/表情/音频顺序，识别隐藏及临时回复 |
| 角色 | 与旧端一致的表情命令、视线和触摸区域，在线语音期间抑制上报 |
| 媒体 | 流式 TTS、唱歌、口型、音量、终止及失败包、本地回放与在线音频优先 |
| 历史 | 分页、去重、阅读锚点；新端缓存可回放，旧端独有音频不承诺恢复 |
| 相处偏好 | 当前字段完整读取、编辑、保存，失败不丢输入 |
| 动态 | 列表、分页、发布、评论、未读及已读；失败保留输入，不误清红点 |
| 自备模型 | 服务端类型需求、LLM/VLM 配置、能力校验、参数透传、异步委托及失败回传 |
| 安装 | 无需 Godot/Python/.NET 的安装包，覆盖安装保留新端数据，卸载默认保留数据 |

## 非目标

### agentluo 0.1.0 交付范围（2026-09-19 确认）

- Windows ZIP 名为 `agentluo-0.1.0.zip`，根目录同名、程序 `agentluo.exe`。版本单一来源，旧交付包保留，用户数据目录继续使用 AgentLuo-Godot。
- 本轮只连接既有公共服务器，不修改或部署服务端。保留旧客户端与手机 App。本轮不含新图片发送、动态图片、点赞、删除、自动更新或本地聊天正文数据库。
- 日志在登录前后独立窗口查看，深色等宽终端风格、中文说明、时间/级别/模块/错误码；可搜索、筛选、复制、暂停跟随、切换启动记录。持续落盘、正常退出结束标记，保留最近 50 次完整启动且保护仍运行实例；选定启动导出完整脱敏诊断 ZIP，不受筛选影响、不上传。写盘失败须明确报告。
- 登录并行连接聊天及请求最近 50 条历史，首批边界建立前发送立即显示但排队。失败可重试或跳过；跳过后本次登录不再导入历史。首批成功按 start_index 每页 50 条向前直到最早；后续失败不阻塞发送，异常分页须显式报告。退出隔离迟到回调，重连不重新抓最新边界。
- UUID 去重和天依实时合并；用户本地 ID 不以文本/时间猜测对应关系。历史不触发表情或声音。首次最新、以后定位本机已读后；定位前不覆盖旧位置，仅前台实际可见推进已读。用户操作后不自动跳转；虚拟列表保持锚点。可见历史图片按 UUID 下载及重试，不信任旧路径、不回写；历史声音仅关联本账号新端完整缓存。
- 相处模式独立非模态窗口：关系、表达风格、自定义值、性格关键词及补充上下文。加载成功才保存；保存前重读并合并修改字段，保留未知字段；默认关系/风格为空值。personality_traits 列表兼容并同步旧 #sym:personality_text；关键词按中英文逗号、顿号及换行分隔。
- 模型用途来自服务端，独立配置启用、服务商、地址、密钥、模型、类型/能力和高级 JSON；复制保留目标要求、重新校验。按服务器/账号/用途隔离、默认 DPAPI，失败仅显式选择明文且默认取消。保存不请求供应商；手动测试告知额度、固定短输入。异步非流式 OpenAI 兼容委托，文本/VLM/JSON 校验、参数优先级、去重、配置快照和退出取消；只回传结果或错误，由服务端决定回退。
- 动态顶部入口与未读（99+）；非模态 QQ 空间式卡片，文字发布、10 条分页、20 条评论分页、评论回复/取消、六行折叠。保留隐私和不可评论规则；登录及每 30 秒查询未读，不自动刷新内容；只有明确全部已读成功后清数。写入超时不自动重发，失败保留草稿。
- 相处/模型窗口及动态草稿纳入关闭窗口、退出账号、退出程序统一放弃确认，默认取消；不持久保存未提交敏感草稿。
- 默认自动测试使用本地 HTTP/WebSocket 和 Fake 供应商。交付另记录 GPU 截图、WASAPI、ZIP/DLL/资源核验，以及未验证的公共服务、人工听感、长期性能和真机范围。

首版不实现电话、移动端、桌宠、托盘、场景探索、昼夜系统、消息撤回/引用/全文搜索、自动更新或新角色。不改造服务端心智，不增加服务端路由或聊天事件。

## 失败行为与数据边界

- 重试复用消息 ID；瞬时输入不阻塞消息队列。明确鉴权失败停止同凭据重连。
- 服务端音频抢占本地回放。接收结束不等于实际播放结束。音频错误保留文字、结束状态且不保存部分失败流。
- 新端使用独立用户数据目录，服务器和账户隔离；退出取消任务并清除自动登录凭据，不混用其他账户缓存。
- API key 默认 DPAPI；保留旧端加密失败后由用户显式选择明文的行为，默认取消，不自动降级。
- 日志不记录密码、token、API key、完整委托提示词或 Base64 音频。

## 验收标准

1. 样板展示长短文本、图片、系统提示、发送各状态、思考、表情、语音状态、空列表及网络错误；真实导出包经用户确认视觉。
2. 自动化通过公开接口检查正常和失败行为；默认离线、无生产配置或计费模型。
3. Windows 10/11、100/125/150/200% DPI、中文及空格路径、中文输入法、窗口与双屏行为完成实际记录。
4. 基准集显机器 1200×800：平均至少 55 FPS，P95 帧时间不超过 33 ms；本地输入反馈小于 100 ms。
5. 连续音频及聊天 30 分钟无持续内存增长或无法结束的播放；受控网络口型偏差目标不超过 150 ms。
6. 1000 条历史仍可输入和滚动；最小化停止非必要绘制，不停止心跳或语音。
7. 发布前实际验证开发服务聊天、图片、TTS、唱歌、动态、自备模型和安装升级；离线测试不得冒充真机或真实服务验收。

## agentluo 0.1.1：独立双栏动态与统一下拉（用户确认）

- 动态为独立任务栏原生系统窗口，聊天最小化后仍显示；初始1000×780、最小900×640，左右45:55可调保存，打开时不自动选择动态。
- 左侧头像/作者/类型与完整时间/一行摘要；右侧完整正文、普通留言、时间升序平铺私人评论。评论仅当前用户与天依，按现有账号权限隔离，不新增楼中楼/图片/赞踩。
- 回复按钮在该评论下展开单个输入框，取消对象保留文字；每条动态保留本窗口草稿与阅读位置。发布用独立非模态小窗口，成功选中新动态；关闭父窗统一确认所有草稿，默认取消，未提交内容不落盘。
- 列表10条、评论20条，滚到底自动分页，失败显式重试。30秒未读查询只提示；用户刷新才更新内容，保留选择/草稿/阅读锚点，评论刷新重读至末页。浏览不标读，全部已读成功才更新。
- 所有正式下拉统一白底/10px圆角/灰边/轻阴影/42px行高；浅灰悬停、#66CCFF选中及键盘焦点，统一稳定ID接口与键盘、边界定位。系统文件对话框和编辑器右键菜单除外。
- 评论7天内相对时间，之后日期，悬停完整时间；无时区公共服务时间按北京时间，异常显示原文。列表与正文完整时间。
- 交付agentluo-0.1.1.zip，保留旧包，不改服务端或旧客户端。真实原生窗口、默认/最小和125%/150%内容缩放、账号隔离及原功能回归分别记录，不以模拟替代真机。

## agentluo 0.1.2：界面以场景绘制（方案 B，用户确认）

- 全部可见界面（账户、聊天、日志/偏好/模型/发布/动态窗口、统一下拉、消息气泡与音频控件、虚拟消息列表、头像面板与预览、离线样板）改为「场景节点树 + 只做行为的脚本」：节点、文案、尺寸、颜色与样式由 `.tscn` 与主题资源提供，脚本只保留行为、信号、原生坐标换算、自绘与既有业务逻辑，不再用代码建树。
- 视图通过公开 `setup(...)` 注入依赖，调用顺序固定 `instantiate() → setup() → add_child()`；既有信号名、公开方法与文案、尺寸、颜色、字号、行为严格等价，本范围不做顺手改进。
- 统一样式收敛为主题与材质资源：`res://theme/app_theme.tres` 是唯一主题来源并作为项目默认主题；圆形头像遮罩与滑块圆点改为资源文件。
- 窗口重设计后，全部可见 UI（含草稿确认和波形）均由 Godot 场景控件承载；只有非 UI 的 Live2D 角色模型及特效仍由驱动运行期创建。
- 验收：新增场景契约测试逐场景断言存在性、根节点类型、关键节点名与 `unique_name_in_owner`、脚本暴露 `setup()` 且不再要求 `_init` 参数；既有 headless 回归与账户/网络/功能分组校验全部通过；相关 GPU 截图与既有基线人工比对，独立的原生窗口仍取到主题（字体、下拉面板圆角与阴影、滑块圆点）。


## 窗口交互与视觉重设计（2026-09-20 用户批准）

本轮替代分立设置窗与独立发布窗的要求。原自绘标题栏及账号入口已由2026-09-21用户选择替代：应用窗口使用系统最小化、最大化/还原、关闭、拖拽及缩放；左窄导航集中聊天、动态、设置、日志四个居中入口，退出登录迁入设置底部。登录前紧凑，登录后角色/聊天45:55可调双栏。浅色轻磨砂仅使用应用内背景，正文实底，8px间距、15px正文字、主要操作至少36px，日志正文保留深色终端风格。

动态和日志独立于主窗最小化，设置跟随主窗。相处/模型合并一个设置窗，跨页保留草稿，整窗保存；先校验所有模型草稿，逐项保存，部分失败明确报告并保留失败项，只有全部成功才允许保存并关闭。密钥不自动降级明文。

发布改动态窗口内浮层；图片为全局单例独立窗口，归属当前图片来源，随其最小化/关闭，换图恢复适应窗口；提供缩放、适应、原始比例和失败重试。关闭动态统一检查发布/评论草稿并关闭附属界面。主窗退出一次汇总设置、聊天和动态草稿，只允许返回编辑或放弃并退出，不自动保存/发送。退出登录关闭业务窗口且清草稿，日志保持；所有文字草稿仅本次运行保留。

重复打开恢复并聚焦已有窗口。窗口分别记忆大小、位置、最大化状态，主窗紧凑/展开分开记忆，不恢复最小化，屏幕变化后约束到可见区。确认框默认取消，Esc/关闭均取消，遮罩点击不关闭。系统文件选择器保留原生。不得新增托盘、跨重启文字草稿或服务端协议。

验收包括实际Windows窗口控制、任务栏/父子联动、保存失败与草稿保护、现有业务回归及GPU截图；真实OS DPI、多屏、Windows10、集显性能未实测时必须明确标注。内容缩放不代替系统DPI验收。

## 0.1.2交付后的用户反馈（2026-09-20）

根据用户12项反馈，正式聊天新增单张图片选择/粘贴、预览、显式发送及失败留稿，替代此前“不提供新图片发送”的限制；继续沿用既有协议。角色恢复默认睁眼、旧端触摸与视线跟随；语音缓存迁入设置并修正确认白框，模型页恢复旧端用途卡片操作；导航对齐加深、应用采用原图标。窗口和下拉真实圆角以对应反馈interface及验证事实为准，不使用裁切区域伪装原生圆角。

## 界面精简与状态修正（2026-09-21用户批准）

删除动态详情的评论隐私标题及其占位，成功评论由内容追加和输入清空体现，不显示成功文案或弹窗；保留加载、失败与结果不确定提示，隐私规则不变。设置移除固定底部说明，状态与操作分行，默认隐藏空状态；保存期间及保存结果可见，再次编辑清除过期成功。退出登录交应用统一处理，保留草稿汇总、取消、保存等待及日志独立规则。

只读输入使用浅色实底且保持不可编辑，失败可重试；API/JSON/thinking开关全状态文字清晰，深蓝#168AC2焦点、浅蓝#D9F1FF选中底、#304553正文。控件仍由场景定义，配色统一主题。不扩展StorageService，不新增业务/UI直接平台调用；历史调用另立专项，不将非存储能力并入StorageService。

验收覆盖真实本地延迟/失败接口、退出草稿和保存等待、GPU加载/保存/恢复及鼠标/键盘开关状态，默认/最小窗口与四档内容缩放的底部按钮可达；真实系统DPI及移动端需单独记录。本轮不升级版本、不覆盖交付包。
