# Godot 0.1.3 登录与气泡

- 目标：白色QQ式登录页、账号历史与独立记住/自动登录、自适应文字及图片气泡，交付Windows x64 0.1.3。
- PRD：[Godot客户端](../需求说明（PRD）/Godot-Windows客户端.md)。
- 总体设计：[客户端总体设计](../../项目说明/项目架构与接口（spec）/Godot客户端总体设计.md)。
- interface：[0.1.3契约](../../项目说明/项目架构与接口（spec）/接口文档/client_godot/release-013.md)。
- 状态：本地实现、验收与Windows x64 0.1.3便携包交付完成；未推送/远端发布，未验证环境见下文。

## 已完成

### 2026-09-21 最终审查：服务器弹层反馈隔离

- SPEC沿用79e0abb的临时服务器编辑与取消规则；Red 3df84ec通过重置表单校验提示→服务器验证→取消实际复现底层提示被覆盖。Green将服务器验证中的反馈限定在弹层，成功切换仍更新登录页，取消保留原表单草稿及提示。
- GPU test_login_presentation通过，包含真实Esc输入、迟到响应、四档内容缩放、小屏、日志复用和登录窗框切换。作者自审仅修改视图状态绑定，无新公开接口或平台调用；独立气泡审查未发现确定缺陷。

### 2026-09-21 登录存储接口

- SPEC 79e0abb；Red 39fb04e实际因五个登录存储能力缺失而失败；Green为本记录提交。StorageService增加profile原子读写和受保护令牌存取/删除，Godot实现复用CredentialStore，磁盘查询不变。
- test_login_storage与原test_storage_service通过：新安装、原子写回、加密令牌、账号/服务器隔离、损坏文件不覆写、不可写路径及不可用基类均验证。JSON数字按值检查，不将引擎解析的浮点数字误判为字段丢失。
- 作者自审：平台API仅在存储实现类，未新增明文降级或修改服务端；原热重载DLL差异未纳入。

### 2026-09-21 登录状态、历史与服务器验证

- SPEC 79e0abb；Red 0337139实际因账号历史/选项/手动令牌登录/服务器切换能力缺失而失败；Green为本记录提交。AccountSession唯一存储依赖改为StorageService，移出所有文件读写；调用者与现有测试显式迁移注入。
- 记住状态与启动自动登录分离，账号/服务器历史隔离，手动与自动令牌轮换、退出仅清当前凭据、取消记住/移除本地记录、旧单账号资料迁移均实现。服务器经既有公钥接口及本地原子保存成功才切换，失败/取消保留原地址。
- 账户组（含新login_profiles）、基础组及功能组均通过。新增测试覆盖普通HTTP失败保留令牌、本地保存失败不换服务器、缺失凭据回密码、只读文件迁移不覆写。独立只读核验与作者自审确认令牌删除失败会显式报告，历史仍可重试移除；不声称文件系统拒绝删除时已清除。无服务端变更。
- 当前引擎真实GPU已确认Window.transparent/transparent_bg及per_pixel_transparency/allowed可用，并导出默认normal、眼睛(1,1)的模型原始截图供静态头像制作；不将该截图当登录外框验收。

### 2026-09-21 白色登录场景与窗口切换

- SPEC 79e0abb；Red 8a23bc7实际因新场景入口/两项勾选/菜单/弹层及旧模式选择器未替换而失败；Green为本记录提交。所有固定控件为Godot场景，头像为已授权模型离线渲染的静态PNG，样式在统一主题，无渐变、矩形裁切或新增原生圆角代码。
- 默认480×690透明圆角小窗，顶部两键、固定三项菜单、服务器验证弹层、账号历史、注册/邀请码重置切页和敏感输入清理实现。记住状态诚实展示且可切回密码；日志跨登录保留，反馈通过独立平台能力，失败给可复制地址。登录后恢复系统窗框/展开几何，退出恢复固定登录形态。
- test_login_presentation使用完整本地HTTP/WebSocket fixture，覆盖菜单/服务器成功与Esc取消迟到响应、重置页草稿保留、记住状态重启及令牌轮换、返回清秘密、日志、浏览器失败、窗框切换、四档内容缩放与320×520小屏可达。账户组、场景/主题、相关功能组与GPU检查均PASS。
- 另外实际复现并修正：无账户服务时顶部退出/诊断入口不能失效；登录存储失败提示不能随登录页隐藏，现由主界面场景提示并在日志记录STORAGE_ERROR。图片存储/其他业务未改。
- 原生窗口探针仅操作自己启动的PID，确认顶栏拖动(36,24)、边缘不缩放、实际菜单截图、关闭结束进程；native-source.json为PASS。高倍测试显式清空固定设计视口尺寸，避免双重缩放。真实系统DPI与Win10仍未验证。
- 资源与验证在artifacts/release-013，头像来源/裁剪参数/SHA写入licenses/RESOURCES.md。作者自审：AccountView和AccountSession无OS/FileAccess/DirAccess/ClassDB直接调用；声明式场景/主题为资源差异，脚本只负责行为，未改服务端或旧端。

### 2026-09-21 文字气泡按排版尺寸增长

- SPEC 79e0abb；Red 3705607实际因短文固定宽度、同字数不同字宽、窄窗URL和字体变化行为失败；Green为本记录提交。文字使用实际字体测量，背景贴合、90%外宽上限并预留头像/间距，长文自动增高。
- 场景增加只传递高度的正文包装，避免旧宽气泡的最小宽度反过来阻止窗口变窄；发送状态和语音条为独立列内容，不撑宽短文背景。未改变文字内容或选择，系统提示保持居中。
- headless与GPU test_bubble_sizing通过，含中英/emoji/换行、长URL、不同字宽、字体变化、状态和语音不撑宽；虚拟千条历史/缩放锚点、系统消息、场景与真实WebSocket语音回归均PASS。作者自审未新增UI代码建树；此片保留图片旧尺寸，图片行为在独立切片验证。

### 2026-09-21 图片原始比例与延迟布局锚点

- SPEC 79e0abb；Red 1fc4dd9实际因缺少原图尺寸、固定135px图片高度、小图放大及长图限高失败；Green为本记录提交。HistoryImages状态提供original_size，磁盘/网络恢复均取真实解码尺寸；缩略图480最长边/24张策略保持。
- 图片控件按原尺寸与90%宽度限制等比缩小，小图不放大、无高度限制，离线纹理同规则；失败显示可重试按钮。图片缩放、长图2400px和原始像素尺寸分别验证，不以缩略图尺寸冒充原图。
- 新增长图实际暴露滚动容器延迟排序将scroll夹到旧max的问题：诊断记录20/offset0变为19/offset1011；现保留待恢复锚点并在容器排序后恢复，实际可见ID同步验证为20及之后。用户交互可取消恢复；清空历史立即返回空锚点。
- 图片headless/GPU、文字气泡、千条历史及HTTP历史媒体均PASS；原图尺寸在网络及磁盘恢复后均校验。作者自审确认未改服务端、原图缓存文件格式或消息协议，临时诊断输出已移除。

### 2026-09-21 发布候选回归与视觉核验

- 基础、账户、网络、功能四组检查全部通过，日志为client_godot/artifacts/release-013/final-{base,accounts,network,features}.log；最终表单修复后重新运行账户组及登录GPU检查（server-cancel-green.log），均PASS。
- GPU实际运行登录、设置六态/延迟失败加载/四档缩放、退出草稿确认、完整应用截图、动态窗口检查；真实HWND检查确认动态/日志独立、设置跟随主窗，均PASS。人工查看登录白色圆角/菜单、角色聊天及设置截图，未发现控件遮挡或旧固定宽气泡。
- 截图工具最初按“设置”标题前缀误选新增的“设置服务器”弹层，真实报错节点为AccountForm/ServerDialog/%PreferencesTab；现按已公开的窗口入口定位，完整GPU截图回归通过。截图工具同时清空固定设计视口，正确测试内容缩放；此项为验收工具修正，不改变产品行为。
- 本机RTX 4070 Laptop、1200×800、90帧采样：应用内磨砂帧时间中位6.073ms/P95 7.549ms，实底6.064ms/P95 7.766ms；静态内存约74.4MB，绘制调用226/225。短样本只作为本机记录，不推断集显性能。
- release.json改为已授权0.1.3，使用说明和测试入口同步，58个test_*.gd已核对。新增UID纳入，既有两项热重载DLL删除保持未暂存。未修改服务端/旧Python客户端，AccountSession/AccountView直接平台API检查为空。
- Win10、真实系统DPI、多屏移除、集显、手机端、公共服务和长期性能未验收；四档测试是内容缩放。所有提交仅本地，未推送或发布。

### 2026-09-21 Windows x64 0.1.3 本地交付

- 发布候选a4b498d，沿用Godot 4.7.1.stable.official.a13da4feb及匹配Windows x64模板，通过scripts/build.ps1 -Package完成import/export/export-startup。构建与运行使用隔离APPDATA，未迁移真实用户资料。
- 程序：`client_godot/dist/agentluo-0.1.3/agentluo.exe`；压缩包：`client_godot/artifacts/agentluo-0.1.3.zip`，43,464,276 bytes，SHA-256 `892be2712873c80932387bce0ea56d191ed0a09d50feafd79fb368ca54e038f9`。
- verify_release_archive.py实际通过ZIP CRC、精确文件清单、全部14文件逐字节SHA-256及版本核验；包内含EXE/PCK、两项原生DLL、许可/资源说明、PREVIEW.md与release.json。逐文件记录在artifacts/release-013/package-manifest.json。图标资源与旧端原始ICO/SVG逐字节一致。
- 原生导出程序实际显示480×690白色圆角登录窗口，运行标题agentluo 0.1.3；持续运行30秒后验证菜单截图、顶栏拖动(36,24)、边缘不可缩放、关闭按钮退出整个进程；native-export.json为PASS，运行日志无引擎错误，已查看native-export.png。
- 保留全部既有交付包，未覆盖同名文件。SPEC/Red/Green及发布候选均已作者自审，本轮完成记录仅本地提交；未创建远端PR或推送，不以独立AI核验替代他人正式审核。
- 本机与离线夹具结果不代表Win10、系统DPI、多屏、集显、手机端、公共服务、长时间运行或签名安装包验收。

### 2026-09-21 QQ参照校准、导出设置空白与偏好重复值修正

- SPEC 3af4a02/a76b3c7；Red 6b567cf/0515552实际加载旧0.1.3 PCK，复现两个隐藏页导出后锚点变为0、模型页0×0/缓存页312×48，以及退出按钮与侧栏宽度不一致。登录主题断言在场景入树后执行，旧PCK仍实际因方形图标、字号与禁用色失败，排除了未入树时主题继承造成的假象。35306ed复现关系/表达风格当前值重复呈现。
- 设置场景对三页显式声明铺满锚点，退出登录宽150px与侧栏对齐。新导出PCK通过默认880×760及最小720×640的所有页尺寸/标题可见性断言；实际release EXE以隔离APPDATA连接本地fixture，使用原生鼠标逐页点击并捕获相处/模型/语音缓存，均可见，退出进程正常。证据在artifacts/qq-ui-review/native-settings.*及对应PNG，已亲自查看。
- 登录页按参考校准120px头像、账号/密码起点226/304px、圆形24px选项图标、18px灰色选项文字、24px黑色输入及白色登录文字、20px底部链接。纯白背景不变，头像改头部取景。请求期间账号/密码/勾选与链接外观保持，登录按钮变浅蓝，操作仍禁用；真实延迟fixture的login-ready/login-busy截图与账户回归通过。SVG仅作为Godot控件主题图标，不创建UI节点或采样桌面。
- 关系/表达风格当前值只显示在输入框，40px箭头打开原预设列表；show_selected_label默认true保持其他下拉行为，菜单选中ID与键盘逻辑不变。GPU设置六态/加载失败/保存/四档内容缩放、退出确认、下拉稳定ID及功能组回归通过。基础、账户、网络组通过，新增偏好修复另跑受影响下拉/设置回归；未改服务端。
- Green为本记录提交。作者自审覆盖场景资源、主题继承、导出序列化、仅登录主题变体的禁用状态、原头像许可及新增测试入口。测试总数59，导出布局为显式GPU入口；未将release模板忽略--script的运行算作测试通过。旧交付ZIP保持原哈希，未纳入热重载DLL变化。

### 2026-09-21 修正版便携包验证

- Green 8fbc518后的实际交付：`client_godot/artifacts/qq-ui-review/agentluo-0.1.3.zip`，43,467,387 bytes，SHA-256 `f33ab49e3134d6cc5c90e007c72c881a7499389ff615e22d7c1540ac2dcb4304`。保留0.1.3版本与原包，使用独立目录提供此次UI修正版；没有覆盖最初的ZIP，旧包SHA仍为892be271…54e038f9。
- 同版本build.ps1完成导入/导出/启动检查；导出PCK默认/最小设置布局复验PASS。release EXE实际完成本地接口自动登录、原生点击三个设置页及关闭；另外白色登录窗持续运行30秒、原生拖动/固定边缘/菜单/关闭检查PASS（native-export-qq.json）。全部14文件CRC/逐文件SHA/版本一致性检查PASS。
- 仅本地提交，无服务端变更或远端发布；Win10、真实系统DPI、多屏、移动端、集显及公共服务仍未验收。

### 2026-09-21 下拉按钮连续点击开关

- SPEC 9262479；Red 8163fe5通过真正的视口鼠标按下/抬起复现第二、四次点击仍展开。信号跟踪表明同一帧button_down后popup_hide，下一帧pressed再次打开；按抬起时的visible判断无法识别本次关闭操作。
- UI内部PopupTrigger记录按下时弹层状态，并协调同次自动隐藏，第二次点击只收起，第三次可重新打开。统一下拉、登录菜单与账号历史复用，未创建可见控件，所有UI仍在场景中。
- GPU连续开/关/开/关、外部点击后再打开、Esc后再打开、键盘选项/禁用项、视口边界及透明圆角均PASS；登录两类弹层的实际鼠标事件和既有账户流程PASS，基础下拉行为PASS。作者自审确认没有定时防抖或全局输入拦截，不吞掉下一次合法点击；Green为本记录提交。

### 2026-09-21 输入聚焦与密码字符规则

- SPEC d41c128；Red 8ab63e7先复现三处聚焦描边，主题修正后继续实际复现中文输入、混合粘贴、确认密码及清空后的校验缓存问题（input-red.log/password-red.log）。账号/密码聚焦现在只保留光标，不关闭键盘焦点能力；移除账号框冗余悬停提示。
- 根据用户明确选择，密码/确认密码只接受可打印ASCII；无效输入恢复整次修改前的值并显示提示，提交前再次校验，不静默过滤后发送。切页、换账号、成功登录及退出时清空校验缓存，不恢复旧密码；账号与邀请码字符规则不变。移动端仅设置Godot密码键盘类型，不声称已真机验证。
- 登录/共享下拉阴影统一集中在主题，使用低透明度8px阴影、中性浅灰边和2px偏移。GPU边界检查发现原定位未计阴影偏移，已补齐可见区预留；默认与缩放下拉、重复点击、键盘、透明圆角全部PASS。
- 登录GPU和账户回归PASS，实际截图login-account-focus/login-password-focus无聚焦框；input-green.log证明拒绝规则及既有登录流程，shadow-green.log证明弹层行为。Green为本记录提交，作者自审确认未新增平台调用、服务端或协议变更，固定界面继续由场景/主题提供。

### 2026-09-21 最新界面修正版交付

- 当前使用本条包，包含Green 8fbc518（视觉与设置页）、2d434f9（连续点击开关）、888c7b9（聚焦/阴影/密码规则）：`client_godot/artifacts/ui-refined/agentluo-0.1.3.zip`，43,465,909 bytes，SHA-256 `e39180310f583562215cc3a906dc83abfb28e5f3eb94dd0697df878f8f4b7621`。旧版与本轮中间包均保留，不升级版本、不覆盖旧文件。
- 最新基础/账户/功能组、登录GPU、下拉GPU通过，网络组在本轮较早已通过且之后未改网络代码。导出import/export/startup通过；实际EXE运行30秒、拖动/固定尺寸/菜单/关闭通过，native-export-ui-refined.json为PASS；ZIP全部14文件CRC/版本/逐文件哈希通过。
- 上一轮鼠标关闭探针曾一次超时，保留native-export-qq-final.json；同一程序复测通过，最新包再次通过，未据此修改关闭逻辑。APPDATA隔离另通过实际OS.get_user_data_dir核实。
- 已用原生鼠标查看导出程序的相处/模型/缓存页与下拉开关序列；最终输入/阴影截图和SPEC/Red/Green作者自审事实见上述记录。仅本地提交，未改服务端；Win10、系统DPI、多屏、手机端和公共服务未实测。

### 2026-09-21 关系/表达风格仅列预设

- SPEC 3160daf，Red a6090c9实际复现两个“自定义”项及非预设值错误选中。移除菜单项和对应旧分支，增加静默清除选择；已保存值和直接输入保持，不自动替换成其它预设。
- GPU设置状态/加载失败/保存/四档缩放、下拉基础行为均PASS；重新导出后原生鼠标打开关系和风格菜单，已无“自定义”，关闭/再打开及退出PASS，进程持续30秒。证据在artifacts/preset-menu-update。Green为本记录提交，已自审，无服务端变更。

### 2026-09-21 设置加载时灰白切换修正

- 根因：PreferencesController加载期间phase=loading，PreferencesPage将输入editable设为false、按钮disabled设为true；主题ReadOnlyInput为#EEF5F9灰蓝底且带边框，与normal纯白无边框不同。服务端响应后phase=ready恢复普通样式，形成先灰后白；不是渲染未完成或窗口失焦。
- SPEC 9778ad1；Red d48b4e3通过本地延迟/失败/保存接口实际复现四个字段及三个按钮的状态颜色差异。相处页增加专用主题变体：只读与普通字段共用白底/文字色，预设和重新加载保持底色；原editable/disabled与状态提示逻辑不变，失败仍允许重试。
- GPU加载/失败/保存/恢复编辑、默认/最小窗口及四档内容缩放PASS；实际采样白底亮度>0.99，同时断言输入不可编辑，证明没有通过提前解锁掩盖问题。已查看settings-loading/settings-saving截图，修正限定相处页。Green为本记录提交，已作者自审。
