# Godot 0.1.2 用户反馈修正

需求来源：用户提供12项反馈及四张截图。沿用Godot场景控件、行为脚本分离；不修改旧客户端。不自动覆盖0.1.2已交付包。

## 有序行为切片

1. 角色默认睁眼：修复眨眼结束后参数未恢复，保留表情的眼睛语义。修改avatar_driver，追加真实模型参数测试及GPU核查。
2. 角色交互：核对原端头/手/身体命中、0.5秒圆环、每秒触摸汇总、在线声音抑制与平滑视线跟随。只在角色驱动内访问Cubism；圆环由场景控件实例化。经会话发送既有user_touch事件。
3. 语音缓存与确认框：复现白框，确认界面具备可见取消/关闭与Esc；将缓存管理迁入设置，保留本账号清理与失败反馈，不改音频缓存保留策略。
4. 模型设置：恢复原端用途卡片列表，启用自备Key才展开；全量预校验、失败留稿与密钥保护明确确认。整窗保存方案已向用户确认，等待答复期间不改依赖该选择的界面。
5. 正式图片发送：场景选图按钮、原生选择器及Ctrl+V接入正式会话；预览后显式发送、取消不发送、校验格式/大小、历史等待屏障、稳定ID重试和账号隔离，沿用user_image协议。
6. 导航、图标及圆角：导航统一对齐、颜色略深；复用原项目SVG/ICO。控件使用Godot圆角资源；系统窗口外轮廓是否接Windows圆角接口已向用户确认，待回答后确定对应SPEC。

每片按公开interface SPEC → Red → Green → 自审与事实记录推进；低影响静态样式与构建配置不制造运行时Red。主代理实现与最终验证，子代理只做定位和独立审查。各片完成后更新本问题状态；完成事实写入开发进度。

## 状态

- 已定位：眨眼参数没有非眨眼期恢复；正式角色无触摸/视线接线；正式聊天缺少图片发送；账号导航左对齐而其它导航居中；原图标来源client/res/gui/icon.svg及icon.ico。
- 已确认旧端：用途卡片启用展开；触摸只作圆环与服务端事件，不直接播放本地动作。图片协议及可靠投递队列可复用。
- 尚待复现：缓存白框实际原生渲染/焦点原因；不能只根据静态信号代码认定问题已修复。
- 尚待用户澄清：系统原生圆角范围、模型页是否保留整窗保存。
